[build]
  command = "npm run build"
  publish = "dist"

# SPA: route all paths to index.html so React Router handles them
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  directory = "netlify/functions"
